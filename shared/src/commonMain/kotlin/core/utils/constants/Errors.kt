package core.utils.constants

class Errors {
    companion object{
        const val BAD_CREDENTIALS = "bad_credentials"
        const val DUPLICATE_FIELD = "duplicate_field"
        const val TOKEN_INVALID = "token_invalid"
    }
}